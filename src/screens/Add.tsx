import * as React from "react";
import { View } from "react-native";
import { Text, Input, Button } from "react-native-elements";

export const Add = () => {
  return (
    <View>
      <Text h1>Add Activity</Text>
    </View>
  );
};

export default Add;
